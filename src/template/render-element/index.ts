import { ConstrainedRenderElementProps } from "~/src/sink"

import { __VarName__Element } from ".."

export function __VarName__({
  element,
  attributes,
  children,
}: ConstrainedRenderElementProps<__VarName__Element>) {
  console.log({ element, attributes, children })
}
