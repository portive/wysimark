module.exports = {
  plugins: {
    /**
     * Ensures that Tailwind CSS is integrated into the PostCSS build process
     */
    tailwindcss: {},
    /**
     * autoprefixer is a PostCSS plugin used to add vendor prefixes to CSS rules
     * automatically. This ensures that your CSS works across different
     * browsers, including those that require specific prefixes for certain
     * properties or features.
     */
    autoprefixer: {},
  },
}
