import { css } from "@emotion/core"

const LINE_COLOR = "#e0e0e0"

export const hrCss = css`
  /**
   * Horizontal Rule
   */

  hr {
    border: none;
    height: 1px;
    background: ${LINE_COLOR};
  }
`
